#ifndef __TABLES__
#define __TABLES__

#include "fixed.h"

Fixed   sin  (Angle angle);
Fixed   cos  (Angle angle);
Fixed   isin (Angle angle);
Fixed   icos (Angle angle);
Fixed   tan  (Angle angle);
Fixed   ctan (Angle angle);

#endif